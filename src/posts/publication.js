import PublicationModel from "../Models/publication";
import MultimediaModel from "../Models/publicacion.multimedia";

async function createPublication(fastify, options) {

    fastify.post('/create-publication', async (request, reply) => {
        try {
            
        } catch (error) {
            
        }
    });
}